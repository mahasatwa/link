ADMIN_USER=admin
# Generate with: npx tsx -e "const b=require('bcryptjs');b.hash('your-password',10).then(console.log)"
ADMIN_PASS_HASH=CHANGE_ME_TO_BCRYPT_HASH
JWT_SECRET=change-me-to-a-secure-random-string

# Local dev (SQLite)
DATABASE_URL=./data/link.db

# Production (Neon Postgres)
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
